using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Driver : Fireman
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //4 AP per turn
    //Fire the Deck Gun: 2 AP
    //When Firing the Deck Gun, the Driver can reroll one or both dice.
    //After rerolling one die, he may then choose to reroll the other, but is limited to a single reroll for each die.
    //Like it or not the second roll always replaces the first.
}
